{
  "name": "e2e-automation-playwright-ts-bdd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "npx ts-node src/helper/report/init.ts",
    "test": "cucumber-js test || true",
    "posttest": "npx ts-node src/helper/report/report.ts",
    "test:failed": "cucumber-js -p rerun @rerun.txt || true",
    "test:invalid-login": "npm run pretest && (cucumber-js test --tags @invalid-login || true) && npm run posttest",
    "test:pim": "npm run pretest && (cucumber-js src/test/features/pim.feature || true) && npm run posttest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.6.0",
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.1.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "fs-extra": "^11.3.3",
    "multiple-cucumber-html-reporter": "^3.9.3",
    "ts-node": "^10.9.2"
  }
}
